# Server configuration
PORT=5000
HOST=0.0.0.0
FLASK_DEBUG=0
SECRET_KEY=change_me_in_production

# Browser settings
HEADLESS=true
BROWSER_TYPE=chromium
BROWSER_CHANNEL=
USER_AGENT_ROTATION=true

# Anti-detection settings
USE_UNDETECTED=true
USE_PROXIES=false
REQUEST_TIMEOUT=30
NAVIGATION_TIMEOUT=60000
WAIT_BEFORE_SCROLLING=2.0
RANDOM_SCROLL_DELAY_MIN=0.5
RANDOM_SCROLL_DELAY_MAX=2.0

# Retry configuration
MAX_RETRIES=3
RETRY_DELAY=2.0

# Proxy configuration (if enabled)
PROXY_TYPE=http
PROXY_HOST=
PROXY_PORT=
PROXY_USERNAME=
PROXY_PASSWORD=

# 2Captcha API key (if used)
CAPTCHA_API_KEY=

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/app.log 